[general]
source_dirs = /home,/etc,/var/www

backup_dir = /var/backups

date_format = %Y-%m-%d

mode = incremental

retention_days = 14

compression = gzip

[rotation]
max_full_backups = 3

max_incrementals = 7

[email]
enabled = true

to = admin@example.com

smtp_server = smtp.mail.local:587

smtp_user = backup_bot
smtp_pass = S3nh@Secr3t

[logging]
log_file = /var/log/backup/backup.log

log_level = INFO