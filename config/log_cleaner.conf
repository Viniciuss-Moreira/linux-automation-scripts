[general]
log_dirs = /var/log,/opt/app/logs

extensions = log,txt

max_age_days = 7

min_size_kb = 100

preserve_patterns = .*error\.log,.*critical\.log

[compression]
enabled = true

method = gzip

[report]
report_file = /var/log/log_cleaner/report-$(date +%F).txt

show_space = true